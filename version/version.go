package version

var (
	// Version is the version number. Filled in at linking time.
	Version = "<unknown>"

	// Revision is the VCS (e.g. git) revision. Filled in at linking time.
	Revision = "<unknown>"
)
